import React from 'react';
import RequestList from '../../AQA/Requests/RequestList';

export default (props) => {

    return (
        <RequestList section='Registry' sectionLink='registry' />
    )
}